from skills import expand_skills_weighted

# weighted = expand_skills_weighted(profile.hard_skills + [profile.current_role])
# # e.g. {"React": 1.0, "JavaScript": 1.0, "Vue": 0.6, "Angular": 0.5}

# # Convert to Qdrant sparse vector format
# # (term → integer index mapping handled by a shared vocabulary)
# sparse_vector = build_sparse_vector(weighted)  # Step 5 detail
